export enum AccessoryCategory {
    PACKAGING = 'PACKAGING',      // Embalagens
    HARDWARE = 'HARDWARE',         // Argolas, parafusos, etc
    FINISHING = 'FINISHING',       // Adesivos, tags NFC, etc
    OTHER = 'OTHER',               // Outros
}
